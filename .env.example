# Copy to .env and fill in. Consumed by the Astro site (step 3+) to query Sanity.
# PUBLIC_ vars are exposed to the built client; the Sanity projectId is not secret.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
# Pin the Sanity API version (YYYY-MM-DD) so query behavior is stable across builds.
PUBLIC_SANITY_API_VERSION=2024-01-01
# Only if the dataset is PRIVATE: a Viewer read token (manage.sanity.io -> API -> Tokens).
# Not PUBLIC_-prefixed, so it stays server/build-side only. Add the same var to the
# host environment (e.g. Cloudflare Pages) for deploys. Never commit the value.
SANITY_API_TOKEN=
